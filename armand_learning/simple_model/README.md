# How does this work?
- `master_script.Rev` co-ordinates the whole model
- `scripts` contains all the components of the total model
- `data` contains the data
- `submission_script.sh` is the job script you submit to the HPC